<?xml version="1.0" encoding="UTF-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/guide_three_bg"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/anim_v3_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_margin="20dp"
        android:text="@string/anim_v3"
        android:textColor="@android:color/darker_gray"
        android:textSize="20sp"
        android:textStyle="italic|bold" />

    <Button
        android:id="@+id/startbutton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="80dp"
        android:background="@drawable/btn_select"
        android:text="@string/start"
        android:textColor="@android:color/darker_gray" />

    <ImageView
        android:id="@+id/anim_ship"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/startbutton"
        android:layout_marginBottom="14dp"
        android:contentDescription="@string/action_settings"
        android:src="@drawable/ic_ship" />

    <ImageView
        android:id="@+id/anim_wave"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/startbutton"
        android:layout_marginBottom="4dp"
        android:layout_marginLeft="-40dp"
        android:layout_marginRight="-40dp"
        android:background="@anim/anim_wave"
        android:contentDescription="@string/app_name" />

</RelativeLayout>